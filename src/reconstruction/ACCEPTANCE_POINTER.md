# See ../deployment/ACCEPTANCE_CONTRACT.md before writing any code that decides
# which records reach the released dataset: never accept on `verdict` alone -
# call verify_sft.acceptance_decision(), which requires format conformance too.
#
# راجع عقد القبول قبل كتابة أي منطق يقرر ما يدخل مجموعة البيانات المُصدَرة.
